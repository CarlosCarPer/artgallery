import { ArtFilterPipe } from './art-filter.pipe';

describe('ArtFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
